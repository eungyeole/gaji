# Rift for Windows

This target will be a packaged WinUI 3 desktop application. Create and build the
solution on Windows so Visual Studio owns packaging, signing, app manifests, and
Windows SDK selection. The UI will consume the same Rift core API as macOS.
